package foroffer.offer;

public class Regex {
    boolean match (char[] str, char[] pattern ){
        if (str.length == 0 || pattern.length == 0)
            return false;
        return matchCore(str, pattern);
    }

    boolean matchCore (char[] str, char[] pattern){


        return false;
    }
}
